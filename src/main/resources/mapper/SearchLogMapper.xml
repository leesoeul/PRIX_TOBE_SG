<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prix.homepage.user.dao.SearchLogMapper">

    <select id="findAll" resultType="com.prix.homepage.user.pojo.SearchLog">
        SELECT id, user_id, title, date, msfile, db, result, engine FROM px_search_log WHERE user_id = #{userId}
    </select>

    <select id="findLogs" resultType="com.prix.homepage.user.pojo.SearchLog">
        SELECT id, user_id, title, date, msfile, db, result, engine FROM px_search_log WHERE user_id = #{userId}
    </select>

    <select id="countLogs" resultType="com.prix.homepage.user.pojo.SearchLog">
        SELECT id, user_id, title, date, msfile, db, result, engine FROM px_search_log WHERE user_id = #{userId}
    </select>

</mapper>